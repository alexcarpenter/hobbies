---
title: Contact
description: I’d love to hear from you — send a note using the form below and I’ll get back to you as soon as I can. If your enquiry is more urgent, send me a message on [Twitter](https://twitter.com/hybrid_alex).
permalink: /contact/
---
{% extends 'layouts/default.njk' %}
{% import "components.njk" as components %}

{% block main %}
  <header>
    <h1 class="{{ tailwind.title }}">{{ title }}</h1>
    {% if description %}
      <div class="prose">
        <p class="text-xl">{{ description | markdownify_inline | safe }}</p>
      </div>
    {% endif %}
  </header>
  <div class="mt-8">
    <form class="w-full max-w-screen-sm" name="contact" netlify-honeypot="bot-field" method="POST" data-netlify="true" action="/contact/thanks/">
      <p class="hidden">
        <label>Don’t fill this out if you're human: <input name="bot-field" /></label>
      </p>
      <div class="flex flex-wrap -mx-3 mb-6">
        <div class="w-full md:w-1/2 px-3 mb-6 md:mb-0">
          <label class="block uppercase tracking-wide text-xs font-bold mb-2" for="name">
            Your name
          </label>
          <input class="appearance-none block w-full bg-white border border-gray-300 py-3 px-4 mb-3 rounded leading-tight focus:outline-none focus:border-green-700" name="name" type="text">
        </div>
        <div class="w-full md:w-1/2 px-3">
          <label class="block uppercase tracking-wide text-xs font-bold mb-2" for="email">
            Your email*
          </label>
          <input class="appearance-none block w-full bg-white border border-gray-300 py-3 px-4 rounded leading-tight focus:outline-none focus:border-green-700" name="email" type="text" required>
        </div>
      </div>
      <div class="flex flex-wrap -mx-3 mb-6">
        <div class="w-full px-3">
          <label class="block uppercase tracking-wide text-xs font-bold mb-2" for="message">
            Message*
          </label>
          <textarea class="appearance-none block w-full bg-white border border-gray-300 py-3 px-4 mb-3 rounded leading-tight focus:outline-none focus:border-green-700" name="message" required></textarea>
        </div>
      </div>
      <div>
        <input type="submit" value="Send message" class="bg-gradient-to-br from-green-700 to-green-800 hover:from-green-800 focus:from-green-800 text-white px-6 py-3 rounded cursor-pointer">
      </div>
    </form>
  </div>
{% endblock %}
